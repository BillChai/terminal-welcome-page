[package]
name = "twp"
version = "0.1.0"
edition = "2024"

[dependencies]
figlet-rs = "0.1.5"
crossterm = { version = "0.29", default-features = false, features = ["events"] }

[dev-dependencies]
portable-pty = "0.9"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
